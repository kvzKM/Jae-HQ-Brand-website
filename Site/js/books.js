import { db } from "./firebase.js";import { collection,getDocs,query,where } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";
const grid=document.getElementById("booksGrid"),clean=v=>String(v??"").trim(),esc=v=>clean(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");function safe(v){try{const u=new URL(clean(v),location.origin);return ["http:","https:"].includes(u.protocol)?u.href:""}catch{return ""}}
async function load(){try{const s=await getDocs(query(collection(db,"books"),where("status","==","published"))),items=s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>Number(!!b.featured)-Number(!!a.featured));if(!items.length){grid.innerHTML='<div class="empty-state" style="grid-column:1/-1">No published books yet. The writing desk is still open.</div>';return}grid.innerHTML=items.map(b=>{const img=safe(b.coverImage),link=safe(b.externalUrl);return `<article class="public-content-card">${img?`<img src="${esc(img)}" alt="${esc(b.title)} cover" loading="lazy">`:'<div class="content-image-placeholder">BK</div>'}<div class="public-content-card-body"><div class="meta">${esc(b.genre||"Writing project")}${b.featured?" · Featured":""}</div><h3>${esc(b.title)}</h3><p>${esc(b.description||"")}</p>${link?`<div class="button-row"><a class="button secondary" href="${esc(link)}" target="_blank" rel="noopener noreferrer">Open book link ↗</a></div>`:""}</div></article>`}).join("")}catch(e){console.error(e);grid.innerHTML='<div class="empty-state" style="grid-column:1/-1">Books are temporarily unavailable.</div>'}}load();
